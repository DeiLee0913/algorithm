## 문제

10진수 수 n이 주어질 때, 이를 b진법으로 변환하여 출력하는 문제입니다.

단, **10 이상인 숫자는 A~Z로 표기**해야 하며, b는 2 이상 36 이하입니다.

## 풀이

### 아이디어

1. **n을 b로 나눈 나머지를 이용하여 각 자릿수 구하기**
    - `n % b`는 b진법의 **가장 낮은 자릿수**를 의미
    - `n /= b`로 `n`을 갱신하며 계속 나머지를 구함
2. **각 자릿수를 문자열에 저장**
    - 0~9: `cnt + '0'`을 이용하여 문자로 변환
    - 10 이상: `cnt + 'A' - 10`으로 대문자 변환
3. **문자열 반전**
    - 계산 과정에서 **가장 낮은 자릿수부터 저장**되므로, 최종적으로 **reverse()**를 사용하여 순서를 반전

### 코드

```cpp
#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int n, b;
    cin >> n >> b;

    string ans = "";

    while (n) {
        int cnt = n % b;
        if (cnt < 10) {
            ans += (cnt + '0');
        } else {
            ans += (cnt + 'A' - 10);
        }
        n /= b;
    }

    reverse(ans.begin(), ans.end());
    cout << ans;

    return 0;
}
```

### 해결 과정에서 어려웠던 점

1. **문자 변환에서 헷갈림**
    - 숫자로 표현 가능한 10 미만의 수와 알파벳으로 표현하는 그 이상의 수를 나누어 문자열에 추가하는 것을 헷갈림
    - `ans += (cnt + '0')` 부분에서 **숫자를 문자로 저장하는 방식**이 익숙하지 않아 시간이 걸림
2. **문자열 반전의 필요성 이해 부족**
    - 계산 과정에서 **가장 작은 자릿수부터 저장**되므로, 최종 결과를 위해 **reverse(ans.begin(), ans.end())**가 필요함을 놓쳐 잘못된 출력 발생

### 발전 가능한 아이디어

**재귀 함수**로 자릿수를 역순으로 출력하면 `reverse()` 없이도 구현 가능