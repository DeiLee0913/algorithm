## 문제

루트가 1인 트리가 주어졌을 때, **각 노드의 부모 노드를 출력**하는 문제입니다.

트리는 무방향 그래프로 주어지며, 노드의 수는 최대 100,000개입니다.

## 풀이

### 아이디어

1. **트리를 인접 리스트로 표현**
    - 무방향 간선이 `n-1`개 주어지므로 인접 리스트(`vector<vector<int>>`)를 사용하여 그래프 구성
2. **DFS 탐색을 통해 부모 노드 기록**
    - 루트 노드(1)에서 시작하여 DFS로 순회하며, 각 자식 노드의 부모를 기록
    - 이미 부모가 정해진 노드는 다시 방문하지 않도록 조건 설정
    - 이 때, 부모 노드를 저장하는 벡터 `parent[i]`를 활용하여 정답 저장

### 코드

```cpp
#include <iostream>
#include <vector>

using namespace std;

void dfs(int here, vector<vector<int>>& adjs, vector<int>& parent) {
    for (int i = 0; i < adjs[here].size(); i++) {
        int there = adjs[here][i];

        // 아직 부모가 정해지지 않은 노드라면
        if (parent[there] == 0) {
            parent[there] = here;
            dfs(there, adjs, parent);
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int n;
    cin >> n;

    vector<vector<int>> adjs(n + 1);

    // 간선 입력
    for (int i = 0; i < n - 1; i++) {
        int a, b;
        cin >> a >> b;
        adjs[a].push_back(b);
        adjs[b].push_back(a);
    }

    vector<int> parent(n + 1, 0); // 각 노드의 부모를 저장할 배열
    dfs(1, adjs, parent); // 루트는 1번 노드

    for (int i = 2; i <= n; i++) {
        cout << parent[i] << "\n";
    }

    return 0;
}

```

### 해결 과정에서 어려웠던 점

1. **간선으로 주어진 트리 정보를 어떻게 정리할지 고민**
    - 트리가 무방향 그래프 형태로 주어지다 보니, 어떤 자료구조로 연결 관계를 표현할지 고민했음.
    - **인접 리스트**를 사용하여 각 노드에 연결된 노드들을 저장하면서 해결.
2. **부모 노드를 어떻게 알아낼 것인가에 대한 아이디어 부족**
    - DFS 탐색 시, 현재 노드가 자식 노드의 부모라는 점을 이용하여
        
        방문하지 않은 노드에 대해 `parent[자식] = 부모`로 기록하는 방법을 힌트로 얻어 해결함.