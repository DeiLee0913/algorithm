## 문제

지구, 태양, 달의 날짜를 기준으로 하는 달력을 사용합니다.

각각의 날짜 범위는 다음과 같습니다:

- 지구(E): 1 ~ 15
- 태양(S): 1 ~ 28
- 달(M): 1 ~ 19

현재 날짜에서 시작해 `(1, 1, 1)`부터 각각의 날짜가 주어졌을 때 `(e, s, m)`을 만족하는 **최소 연도**를 구하는 문제입니다.

## 풀이

### 아이디어

- **모든 날짜를 1부터 증가시키며 조건 만족 여부 확인**
    - 각 날짜는 특정 주기를 가지고 반복됩니다.
    - **모듈로 연산** 또는 **증가 후 리셋**을 통해 날짜를 순환시킴.
- **while 루프를 활용하여 가장 먼저 `(e_, s_, m_) == (e, s, m)`을 만족하는 시점 반환**

### 코드

```cpp
#include <iostream>
using namespace std;

int min_rotate(int e, int s, int m) {
    int e_ = 1, s_ = 1, m_ = 1;
    int cnt = 1;

    while (true) {
        if (e_ == e && s_ == s && m_ == m) {
            break;
        }

        e_++; s_++; m_++; cnt++;

        if (e_ > 15) e_ = 1;
        if (s_ > 28) s_ = 1;
        if (m_ > 19) m_ = 1;
    }

    return cnt;
}

int main() {
    int e, s, m;
    cin >> e >> s >> m;

    cout << min_rotate(e, s, m);

    return 0;
}

```

### 해결 과정에서 어려웠던 점

1. **0 처리 방식의 혼란**
    - 처음에는 `%` 연산으로 `0`이 나올 경우 `15`, `28`, `19`와 비교하는 복잡한 로직을 사용했으나,
        
        `1부터 시작하고 최대값을 넘으면 다시 1로 초기화`하면 훨씬 간단해짐.
        

### 발전 가능한 아이디어

1. **수학적 접근으로 시간 단축**
    - 문제의 범위가 작기 때문에 현재 방식으로 충분하지만,
        
        **공통 주기의 최소값을 찾는 방식**으로도 해결 가능 (예: 최소공배수 활용).