## 문제

두 개의 자연수가 주어졌을 때, 그 두 수의 최대공약수(GCD)와 최소공배수(LCM)를 구하는 문제입니다.
입력으로 주어진 두 수는 1만 이하의 자연수입니다.

## 풀이

### 아이디어

1. **최대공약수(GCD)**:
    - **유클리드 호제법**을 사용하여 두 수의 최대공약수를 효율적으로 구합니다.
    - `gcd(a, b) = gcd(b, a % b)`를 반복하다가 `b = 0`이 되면 `a`가 최대공약수입니다.
2. **최소공배수(LCM)**:
    - 두 수의 곱을 최대공약수로 나누면 최소공배수를 구할 수 있습니다.
    - 공식: `lcm(a, b) = a / gcd(a, b) * b`
    - ❗ **주의**: `(a * b) / gcd(a, b)`로 직접 계산하면 **오버플로우**가 발생할 수 있으므로
        
        `a / gcd(a, b)`를 먼저 계산한 뒤 `* b`를 하는 방식으로 처리합니다.
        

### 코드

```cpp
#include <iostream>

using namespace std;

// 유클리드 호제법을 이용한 최대공약수
int gcd(int a, int b) {
	while (b != 0) {
		int tmp = a % b;
		a = b;
		b = tmp;
	}
	return a;
}

// 최소공배수
int lcm(int a, int b) {
	return a / gcd(a, b) * b;
}

int main() {
	int a, b;
	cin >> a >> b;

	cout << gcd(a, b) << "\n" << lcm(a, b);

	return 0;
}
```

### 해결 과정에서 어려웠던 점

1. **최소공배수 계산 시 오버플로우 주의**
    - 처음에는 `a * b / gcd(a, b)`로 계산했으나, **입력값이 클 경우 오버플로우 위험**이 있음
    - 이를 방지하기 위해 **`a / gcd(a, b)`를 먼저 계산**한 후 `b`를 수행하여 문제 해결

### 발전 가능한 아이디어

- C++17 이상에서는 `<numeric>` 헤더의 `gcd`, `lcm` 함수를 사용할 수 있음.
    
    ```cpp
    #include <iostream>
    #include <numeric>
    
    using namespace std;
    
    int main() {
    	int a, b;
    	cin >> a >> b;
    
    	cout << gcd(a, b) << "\n" << lcm(a, b);
    
    	return 0;
    }
    ```